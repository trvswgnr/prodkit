{
  "name": "@prodkit/examples",
  "private": true,
  "description": "Consumer-level smoke checks and runnable examples for @prodkit/op and @prodkit/std.",
  "type": "module",
  "scripts": {
    "fmt": "oxfmt \"**/*.ts\"",
    "fmt:check": "oxfmt --check \"**/*.ts\"",
    "lint": "oxlint --config ../oxlint.config.ts .",
    "lint:fix": "oxlint --config ../oxlint.config.ts --fix .",
    "smoke": "node ./smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prodkit/op": "workspace:*",
    "@prodkit/std": "workspace:*",
    "better-result": "2.9.0",
    "valibot": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=24.14.0"
  }
}
