{
  "name": "@prodkit/monorepo",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=24.14.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "fmt": "turbo run fmt",
    "fmt:check": "turbo run fmt:check",
    "check": "turbo run check",
    "bench": "turbo run bench --filter=@prodkit/op-benchmarks",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.9.0"
  }
}
